#include <stdio.h>
#include <math.h>

int main()
{
    float x = 45;

    // x = sqrt(x);
    // x = pow(x, 2);
    // x = round(x);
    // x = abs(x);
    // x = sin(x);
    // x = cos(x);
    x = tan(x);

    printf("%f", x);

    return 0;
}